# Runbook — <title>

- **Trigger:** what situation or request starts this procedure.
- **Owner:** role responsible. **Severity/urgency:** if incident-related.

## Preconditions

- Access, tools, env (`.env` rendered, `bw`/GitHub secrets available), services expected up.

## Steps

1. Concrete, copy-pasteable commands (`make …` / `just …` / `gh …`).
2. …

## Verify

- How to confirm success (expected output, a green Prefect run, a row count, a file present).

## Rollback

- How to undo safely if a step fails.

## Escalation

- Who/what to contact if blocked (note: infra/DevOps is handled separately).
