name: ogip-dbt
prefect-version: 3.0.0
deployments:
  - name: ingest-transform-publish-dbt
    entrypoint: pipelines/dbt/flow.py:flow
    work_pool:
      name: default
    schedule:
      cron: "0 6 * * *"
      timezone: UTC
