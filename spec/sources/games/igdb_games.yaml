# ── GENERATED FILE — DO NOT EDIT ──────────────────────────────────────────────
# Projected from the ingestion registry (the SSoT):
#   ~/.ai/skills/.settings/de/ingestion/sources/games/igdb_games.yml
# Edit THAT file, then regenerate:
#   just -f ~/.ai/skills/_scripts/de/ingestion/Justfile spec-emit /Users/nk.myg/gi/@dataengy/OGIP
# Hand edits here WILL be overwritten and were measured to be unsyncable by
# hardlink (git checkout severs the link) — hence this one-way projection.
# ──────────────────────────────────────────────────────────────────────────────
# Source: igdb_games
# Split from ingestion.yml — edit HERE, this is the SSoT.
  igdb_games:
    system: igdb
    entity: games
    # The entity-resolution backbone (engine, franchises, companies) — PLAN A6 committed
    # core. NOTE: auth is oauth2 (Twitch client-credentials), which the probe does NOT yet
    # implement; it will report KEY-MISSING rather than a false green. That is the correct
    # failure: a claim this registry cannot verify must not look verified.
    url: https://api.igdb.com/v4/games
    auth: oauth2
    tier: direct
    publishable: false
    license: igdb-api-tos
    license_note: >
      IGDB (Twitch/Amazon) ToS permits API use with attribution but does not grant bulk
      redistribution. Connector public, data local — same posture as RAWG.
    key_param: null # bearer token in Authorization header, not a query param
    slot: IGDB_CLIENT_ID # paired with IGDB_CLIENT_SECRET for the token exchange
    records_path: null # returns a bare JSON array
    probe_params: {}
    rate_limit_note: "4 requests/second; token from https://id.twitch.tv/oauth2/token (client_credentials)"
    traps: >
      Query body is APICalypse (a custom DSL), POSTed as the raw body — NOT JSON, NOT query
      params. A GET with query params returns 400. This is why the one-GET probe cannot
      verify it and honestly reports KEY-MISSING instead.
    dlt:
      resource: igdb__games
      primary_key: id
      write_disposition: merge
    docs: https://api-docs.igdb.com/
